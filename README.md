# recuperate-skill

A Claude Code skill that builds a cached, comprehensive understanding of a project — internal file dependencies, external libraries, a full-text search index, public API surface, per-line logic, per-domain implementation context, and git/ticket history — as the foundation for creating an implementation roadmap.

See `SKILL.md` for the workflow and `references/` for each crawler agent's full role definition.
